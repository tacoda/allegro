[package]
name = "presto"
version = "0.1.0"
edition = "2021"
description = "An Elixir-flavored functional language for composing AI agents"

[dependencies]
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
serde_json = "1"

[[bin]]
name = "presto"
path = "src/main.rs"
