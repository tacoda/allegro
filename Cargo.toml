[package]
name = "presto"
version = "0.1.0"
edition = "2021"
description = "Presto — a language to easily build agent harnesses"

[dependencies]
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
serde_json = "1"

[[bin]]
name = "presto"
path = "src/main.rs"
